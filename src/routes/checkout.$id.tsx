import { createFileRoute, useNavigate, Navigate, Link } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { SiteLayout } from "@/components/site/SiteLayout";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { PickupWindowAlert } from "@/components/orders/PickupWindowAlert";
import { supabase } from "@/integrations/supabase/client";
import { useAuth } from "@/hooks/use-auth";
import { sampleListings, formatBND } from "@/lib/sample-data";
import { AlertTriangle, Minus, Plus } from "lucide-react";
import { toast } from "sonner";

export const Route = createFileRoute("/checkout/$id")({ component: Checkout });

const genCode = () => Math.random().toString(36).slice(2, 8).toUpperCase();
const isPastPickup = (pickupEnd: string) => { const end = new Date(pickupEnd); return !Number.isNaN(end.getTime()) && end.getTime() < Date.now(); };
const toTimeInput = (value?: string | null) => { if (!value) return ""; if (value.length >= 5 && value[2] === ":") return value.slice(0, 5); const d = new Date(value); if (Number.isNaN(d.getTime())) return ""; return `${String(d.getHours()).padStart(2, "0")}:${String(d.getMinutes()).padStart(2, "0")}`; };
const toMinutes = (value: string) => { const [h, m] = value.split(":").map(Number); return h * 60 + m; };
const formatTime = (t: string) => { const raw = toTimeInput(t); if (!raw) return ""; const [h, m] = raw.split(":").map(Number); const hour12 = h % 12 || 12; return `${hour12}:${String(m).padStart(2, "0")} ${h >= 12 ? "PM" : "AM"}`; };
const buildPickupTime = (hour: string, minute: string, period: string) => { if (!hour || !minute || !period) return null; const h = Number(hour); const m = Number(minute); if (!Number.isInteger(h) || !Number.isInteger(m) || h < 1 || h > 12 || m < 0 || m > 59) return null; let hour24 = h; if (period === "PM" && h !== 12) hour24 += 12; if (period === "AM" && h === 12) hour24 = 0; return `${String(hour24).padStart(2, "0")}:${String(m).padStart(2, "0")}`; };
const formatPickupParts = (hour: string, minute: string, period: string) => { const built = buildPickupTime(hour, minute, period); return built ? formatTime(built) : ""; };

function Checkout() {
  const { id } = Route.useParams();
  const { user, isAuthenticated, loading } = useAuth();
  const navigate = useNavigate();
  const [listing, setListing] = useState<any>(null);
  const [listingLoading, setListingLoading] = useState(true);
  const [listingError, setListingError] = useState<string | null>(null);
  const [qty, setQty] = useState(1);
  const [placing, setPlacing] = useState(false);
  const [pickupHour, setPickupHour] = useState("");
  const [pickupMinute, setPickupMinute] = useState("");
  const [pickupPeriod, setPickupPeriod] = useState<"AM" | "PM" | "">("");

  const resetPickupTime = () => { setPickupHour(""); setPickupMinute(""); setPickupPeriod(""); };

  useEffect(() => { let cancelled = false; (async () => { setListingLoading(true); setListingError(null); resetPickupTime(); const local = sampleListings.find((l) => l.id === id); if (local) { if (!cancelled) { setListing(local); setListingLoading(false); } return; } const { data, error } = await (supabase as any).from("listings").select("*").eq("id", id).maybeSingle(); if (cancelled) return; if (error) { setListing(null); setListingError(error.message); setListingLoading(false); return; } if (!data) { setListing(null); setListingLoading(false); return; } const { data: merchant } = await (supabase as any).from("merchants_public").select("business_name, district, rating, business_type").eq("id", data.merchant_id).maybeSingle(); setListing({ ...data, merchant: { business_name: merchant?.business_name ?? "", district: merchant?.district ?? "", rating: Number(merchant?.rating ?? 0), business_type: merchant?.business_type ?? "" } }); setListingLoading(false); })(); return () => { cancelled = true; }; }, [id]);

  if (loading) return <SiteLayout><div className="p-6 sm:p-10">Loading…</div></SiteLayout>;
  if (!isAuthenticated) return <Navigate to="/auth" search={{ redirect: `/checkout/${id}` }} />;
  if (listingLoading) return <SiteLayout><div className="p-6 sm:p-10">Loading listing…</div></SiteLayout>;
  if (!listing) return <SiteLayout><div className="container mx-auto p-6 text-center sm:p-10"><h1 className="text-2xl font-bold">Listing not found</h1><p className="mx-auto mt-2 max-w-md text-sm text-muted-foreground">{listingError ? `We couldn't load this listing: ${listingError}` : "This listing may have been removed or is no longer available."}</p><Button asChild className="mt-4 rounded-full"><Link to="/browse">Back to browse</Link></Button></div></SiteLayout>;

  const expired = isPastPickup(listing.pickup_end);
  if (expired) return <SiteLayout><div className="container mx-auto p-6 text-center sm:p-10"><h1 className="text-2xl font-bold">Offer expired</h1><p className="mx-auto mt-2 max-w-md text-sm text-muted-foreground">The pickup window for this offer has ended, so it can no longer be reserved.</p><Button asChild className="mt-4 rounded-full"><Link to="/browse">Browse other food</Link></Button></div></SiteLayout>;

  const max = Math.max(1, Math.min(listing.quantity_available ?? 1, 5));
  const total = Number(listing.discounted_price) * qty;
  const pickupStart = toTimeInput(listing.pickup_start);
  const pickupEnd = toTimeInput(listing.pickup_end);
  const pickupTimeValue = buildPickupTime(pickupHour, pickupMinute, pickupPeriod);
  const pickupDisplay = formatPickupParts(pickupHour, pickupMinute, pickupPeriod);
  const pickupComplete = Boolean(pickupHour && pickupMinute && pickupPeriod);
  const pickupValid = Boolean(pickupTimeValue) && toMinutes(pickupTimeValue!) >= toMinutes(pickupStart) && toMinutes(pickupTimeValue!) <= toMinutes(pickupEnd);

  const place = async () => { if (!user) return; if (isPastPickup(listing.pickup_end)) { toast.error("This offer has expired."); return; } if (!pickupComplete) { toast.error("Please fill in the pickup hour, minute, and AM/PM before confirming."); return; } if (!pickupTimeValue) { toast.error("Please enter a valid pickup time."); return; } if (!pickupValid) { toast.error(`Choose a pickup time between ${formatTime(listing.pickup_start)} and ${formatTime(listing.pickup_end)}.`); return; } setPlacing(true); if (!listing.merchant_id) { setPlacing(false); const fakeCode = genCode(); toast.success("Reservation confirmed — collect only at your selected pickup time."); navigate({ to: "/order/$id", params: { id: `demo-${fakeCode}` }, search: { code: fakeCode, demo: 1, pickupTime: pickupDisplay } as any }); return; } const pickup_code = genCode(); const { data, error } = await (supabase as any).from("orders").insert({ user_id: user.id, listing_id: listing.id, merchant_id: listing.merchant_id, quantity: qty, total_price: total, commission_amount: 0, merchant_payout: total, pickup_code, status: "reserved", payment_status: "pending", payment_method: `pay_at_pickup|pickup_time=${pickupDisplay}` }).select("id").single(); if (!error && data) await supabase.from("listings").update({ quantity_available: Math.max(0, listing.quantity_available - qty) }).eq("id", listing.id); setPlacing(false); if (error) return toast.error(error.message); toast.success("Reservation confirmed — collect only at your selected pickup time."); navigate({ to: "/order/$id", params: { id: data!.id }, search: { pickupTime: pickupDisplay } as any }); };

  return <SiteLayout><section className="container mx-auto grid max-w-4xl gap-6 px-3 py-6 pb-24 sm:px-4 sm:py-10 md:grid-cols-[1.2fr_1fr] md:gap-8 md:pb-10"><div className="space-y-5 sm:space-y-6"><h1 className="text-3xl font-bold leading-tight">Checkout</h1><PickupWindowAlert pickupStart={listing.pickup_start} pickupEnd={listing.pickup_end} /><Card className="rounded-3xl p-4 sm:p-5"><div className="flex items-center gap-3 sm:gap-4"><img src={listing.image_url} className="h-20 w-20 shrink-0 rounded-xl object-cover" alt="" /><div className="min-w-0"><div className="line-clamp-2 font-semibold leading-tight">{listing.title}</div><div className="truncate text-sm text-muted-foreground">{listing.merchant?.business_name ?? listing.merchants?.business_name}</div></div></div><div className="mt-5 flex items-center justify-between gap-3"><span className="text-sm font-medium">Quantity</span><div className="flex items-center gap-2"><Button size="icon" variant="outline" className="h-10 w-10 rounded-full" onClick={() => setQty(Math.max(1, qty - 1))}><Minus className="h-4 w-4" /></Button><span className="w-8 text-center font-semibold">{qty}</span><Button size="icon" variant="outline" className="h-10 w-10 rounded-full" onClick={() => setQty(Math.min(max, qty + 1))}><Plus className="h-4 w-4" /></Button></div></div><div className="mt-4 rounded-2xl bg-cream/60 p-4 text-sm">Pickup window: <strong>{formatTime(listing.pickup_start)} – {formatTime(listing.pickup_end)}</strong></div><div className="mt-4 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900"><div className="flex items-start gap-2 font-bold"><AlertTriangle className="mt-0.5 h-4 w-4 shrink-0" /> Required: fill in your pickup time</div><p className="mt-2 text-xs font-medium">Use the time fields below to choose a time within the merchant's pickup window. Leave nothing blank before confirming.</p></div><div className="mt-4 grid gap-2"><Label className="text-base font-bold">Your pickup time <span className="text-destructive">*</span></Label><div className="grid grid-cols-[1fr_auto_1fr] items-center gap-2"><div><Label htmlFor="pickup-hour" className="sr-only">Hour</Label><Input id="pickup-hour" type="number" inputMode="numeric" min={1} max={12} placeholder="HH" value={pickupHour} onChange={(e) => setPickupHour(e.target.value.replace(/\D/g, "").slice(0, 2))} className={!pickupHour ? "border-amber-400 ring-2 ring-amber-200" : ""} /></div><span className="text-lg font-bold">:</span><div><Label htmlFor="pickup-minute" className="sr-only">Minute</Label><Input id="pickup-minute" type="number" inputMode="numeric" min={0} max={59} placeholder="MM" value={pickupMinute} onChange={(e) => setPickupMinute(e.target.value.replace(/\D/g, "").slice(0, 2))} onBlur={() => pickupMinute && setPickupMinute((m) => String(Math.min(59, Math.max(0, Number(m)))).padStart(2, "0"))} className={!pickupMinute ? "border-amber-400 ring-2 ring-amber-200" : ""} /></div></div><div className="grid grid-cols-2 gap-2"><Button type="button" variant={pickupPeriod === "AM" ? "default" : "outline"} className="rounded-full" onClick={() => setPickupPeriod("AM")}>AM</Button><Button type="button" variant={pickupPeriod === "PM" ? "default" : "outline"} className="rounded-full" onClick={() => setPickupPeriod("PM")}>PM</Button></div><p className="text-xs font-medium text-amber-700">Important: choose a time between {formatTime(listing.pickup_start)} and {formatTime(listing.pickup_end)}. Food can only be collected at your selected pickup time.</p></div></Card><Card className="rounded-3xl p-4 sm:p-5"><h2 className="font-semibold">Payment</h2><p className="mt-2 text-sm text-muted-foreground">You'll pay the merchant when you collect your order.</p></Card></div><aside className="hidden md:block"><Card className="sticky top-24 rounded-3xl p-6"><OrderSummary qty={qty} total={total} pickupTime={pickupDisplay} placing={placing} place={place} disabled={!pickupValid} /></Card></aside></section><div className="fixed inset-x-0 bottom-0 z-40 border-t bg-background/95 p-3 shadow-2xl backdrop-blur md:hidden"><div className="mx-auto flex max-w-xl items-center justify-between gap-3"><div><div className="text-xs text-muted-foreground">Total at pickup</div><div className="text-2xl font-bold leading-none text-primary">{formatBND(total)}</div>{pickupValid ? <div className="mt-1 text-xs font-medium text-amber-700">Pickup at {pickupDisplay}</div> : <div className="mt-1 text-xs font-bold text-amber-700">Fill pickup time first</div>}</div><Button onClick={place} disabled={placing || !pickupValid} className="h-12 rounded-full px-6">{placing ? "Confirming…" : pickupValid ? "Confirm" : "Set time"}</Button></div></div></SiteLayout>;
}

function OrderSummary({ qty, total, pickupTime, placing, place, disabled }: { qty: number; total: number; pickupTime: string; placing: boolean; place: () => void; disabled: boolean }) { return <><h2 className="font-semibold">Order summary</h2><Row label={`Subtotal × ${qty}`} value={formatBND(total)} />{!disabled ? <Row label="Pickup time" value={pickupTime} /> : <Row label={<span className="font-bold text-amber-700">Pickup time required</span>} value={<span className="font-bold text-amber-700">Not selected</span>} />}<Row label="Service fee" value="B$0.00" /><div className="my-3 h-px bg-border" /><p className="rounded-xl bg-amber-50 px-3 py-2 text-xs font-medium text-amber-800">You must fill in a pickup time within the pickup window before confirming.</p><Row label={<span className="font-semibold">Total to pay at pickup</span>} value={<span className="text-lg font-bold">{formatBND(total)}</span>} /><Button onClick={place} disabled={placing || disabled} className="mt-5 w-full rounded-full" size="lg">{placing ? "Confirming…" : !disabled ? "Confirm reservation" : "Set pickup time first"}</Button><p className="mt-3 text-xs text-muted-foreground">No payment is taken online.</p></>; }
function Row({ label, value }: { label: React.ReactNode; value: React.ReactNode }) { return <div className="mt-2 flex items-center justify-between gap-3 text-sm">{label}<span className="text-right">{value}</span></div>; }
